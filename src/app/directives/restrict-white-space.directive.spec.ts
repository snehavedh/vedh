import { RestrictWhiteSpaceDirective } from './restrict-white-space.directive';

describe('RestrictWhiteSpaceDirective', () => {
  it('should create an instance', () => {
    const directive = new RestrictWhiteSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
