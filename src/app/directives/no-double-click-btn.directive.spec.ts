import { NoDoubleClickBtnDirective } from './no-double-click-btn.directive';

describe('NoDoubleClickBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new NoDoubleClickBtnDirective();
    expect(directive).toBeTruthy();
  });
});
