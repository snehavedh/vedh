import { TrackCapslockDirective } from './track-capslock.directive';

describe('TrackCapslockDirective', () => {
  it('should create an instance', () => {
    const directive = new TrackCapslockDirective();
    expect(directive).toBeTruthy();
  });
});
